{
  "name": "monsocket",
  "private": true,
  "description": "Socket.io for Monad — realtime multiplayer rooms where every action is an onchain transaction",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "compile": "node scripts/compile.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "solc": "^0.8.28",
    "typescript": "^5.8.3",
    "vite": "^6.0.0"
  }
}
