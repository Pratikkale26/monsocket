# Copy to .env and fill in. .env is gitignored.

# deltaV (deltav.monad.xyz) — scoped to a startup profile, used to post
# weekly updates from scripts. Generate at deltav.monad.xyz/dashboard.
DELTAV_API_KEY=
